{
  "name": "breachwatcher-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy et cache KV pour BreachWatcher",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

